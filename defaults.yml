# ---------Default variables-------------
# Change variables to fit your needs

# !!! Change this to your username to use docker without sudo
docker_user: 'DEFAULT_USER'

# Need to set the docker-compose version manually
docker_compose_version: '1.21.0'
